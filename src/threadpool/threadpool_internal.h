/* threadpool_internal.h — private to the `threadpool` module.
 *
 * Nothing here is visible to other modules. Implementation details of the pool
 * (queue layout, worker record) live here as the module grows.
 */
#ifndef HB_THREADPOOL_INTERNAL_H
#define HB_THREADPOOL_INTERNAL_H

#include "threadpool/threadpool.h"

/* (implementation details are file-local in threadpool.c) */

#endif /* HB_THREADPOOL_INTERNAL_H */
